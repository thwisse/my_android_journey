package io.github.thwisse.kekodlessonone

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle

class MainActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)

        // build.gradle.kts -> libs.versions.toml

        // codelabs hepsini coz

        // ai coding assistant
        // .ignore

        // android developers youtube channel

        // manifest -> merged manifest

        // gitignore turuncu dosyalar vs git'e eklenmez

        // ctrl alt shift L ile kod oto duzenleme
        // gradle.properties -> kotlin.code.style ekipteki herkeste ayni olmali yoksa bu kisayol
        // yuzunden karisiklik cikar

        // dependency graph tool

        // resource manager ile kaynaklar detayli incelenebilir. drawble klasorunden bakmaya calisma

        // onemli seyleri bookmarks'a ekleyebilirsin.

        // bazi onemli takim ici infolari, teknik borclari vs. TO DO ile belirt

        // build variants kisminda x ve y lerin kartezyen carpimi kadar varyant olusur.

        // documentation note
        /**
         * selamlar
         * sevgiler
         */

        // app quality insights -> andorid vitals kismindan kendi uygulamanin istetisliklerini
        // gorebilirsin

        // firebase crashlytics kismini da enable et kod yazarken sana crash infosu verir.

        // analyze -> inspect code -> whole project
        // projenin inspection'larini incelersin ve su an sikinti yaratmayan ama ilerde sikinti
        // yaratabilecek seyleri gorursun duzeltirsin.

        // local history -> show history
        // git disinda da locak history de var ide icinde. onu da kullanabilirsin gerektiginde

        // kablosuz hata ayiklama wifi ile kullanirsin

        // gemini kullan ide icinde
        // hesabinla giris yap

        // tool windows ile olmayan windowlari gorebilirsin

        // running devices kismindan emulatoru ya da bagli cihazi acip uygualamyi calistirdiktan
        // sonra layout inpector ile calisan uygulama uzerinde viewleri, componentleri ve ozelliklerini
        // inceleyebilirsin. hatta katman katman gormek bile mumkun.

        // developer.android.com




    }
}